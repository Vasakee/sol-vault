// Integration tests for sol-vault — run with `anchor test`
// TypeScript tests in tests/ folder cover the full flow.
// This file is intentionally minimal to allow `anchor build` to succeed.

#[test]
fn placeholder() {
    // Real tests are in the TypeScript test suite (anchor test)
    assert!(true);
}
